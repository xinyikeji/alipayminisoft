<view>
  <notice enableMarquee="{{true}}">自助点餐送可乐一杯(限每月一杯)！        
  </notice>
  <message title="{{title}}" subTitle="{{subTitle}}" type="success" subButton="{{messageButton.subButton}}" mainButton="{{messageButton.mainButton}}" onTapMain="showQrcode">
  </message>
  <button type="default" onTap="clearCache">清理缓存</button>
</view>